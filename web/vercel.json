{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "dist/spa",
    "installCommand": "npm install",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/index.html"
      }
    ]
  }